yarn lintfix && git add . && git commit --allow-empty -m "$1" && git push && ssh arenatickets << EOF
  sudo -i
  cd /var/www/arenatickets_widget;
  git pull;
  yarn;
  yarn lintfix;
  yarn build;
  pm2 reload ArenaticketsWidget;
EOF
